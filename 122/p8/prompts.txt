Prompt: I just completed a pre-CS 1 university Python programming class. We learned through lists, with a very brief introduction to dictionaries. Please produce a list of possible Python projects that I might create with your help, within the limits of the course, with a special emphasis on lists, and a light usage of dictionaries. I would like to start out with a list of potential projects and no code.

Project:Playlist Organizer 
    - Store songs as list items 
    - Add/remove/reorder tracks
    - Use a small dictionary for song metadata (e.g., title, artist)



Prompt: explain (without code) how 1 will use Python lists and dictionaries

AI Explaination: 
    Lists: Store the playlist, Manage order, Add/remove/rearrange songs, Loop through songs

    Dictionaries: Store details about each song, Allow structured information, Enable simple searching and filtering


Outline Prompt: create an outline for the coding steps to create this project.

AI Outline: 
    ✅ 1. Set up the basic data structures
    Create an empty list that will hold all songs in the playlist.

    Decide what information each song will store (e.g., title, artist).

    Represent each song as a dictionary with keys like "title" and "artist".

    ✅ 2. Build the main program loop
    Create a loop that keeps the program running until the user chooses to quit.

    Inside the loop, show a menu of actions (add song, remove song, view playlist, etc.).

    Ask the user for a choice and respond accordingly.

    ✅ 3. Implement “Add a Song”
    Ask the user for the song’s details (title, artist, etc.).

    Create a dictionary using those details.

    Append that dictionary to the playlist list.

    Confirm to the user that the song was added.

    ✅ 4. Implement “View Playlist”
    If the playlist is empty, display a message.

    Otherwise, loop through the list and show each song in order.

    Display the song number (its index + 1) so the user can refer to it later.

    ✅ 5. Implement “Remove a Song”
    Show the playlist with numbers.

    Ask the user which song number they want to remove.

    Convert that number to the correct list index.

    Remove the song from the list.

    Confirm removal.

    ✅ 6. Implement “Reorder Songs” (optional but great practice)
    Show the playlist with numbers.

    Ask the user which song they want to move.

    Ask where they want to move it.

    Remove the song from its current position.

    Insert it into the new position.

    ✅ 7. Implement “Search for Songs” (optional)
    Ask the user what they want to search by (title or artist).

    Loop through the playlist and check each song’s dictionary values.

    Display any matches.

    ✅ 8. Implement “Show Song Details” (optional)
    Show the playlist with numbers.

    Ask the user which song they want details for.

    Display all key–value pairs from that song’s dictionary.

    ✅ 9. Add data validation (beginner‑friendly level)
    Make sure the user enters valid menu choices.

    Make sure song numbers are within the playlist range.

    Handle empty playlist situations gracefully.

    ✅ 10. Add a “Quit” option
    When the user chooses to quit, exit the loop.

    Optionally display a goodbye message.